#подключаем модуль ОС
import os
#прописываем путь к фолдеру (обозначем переменную path)
path = "C:\Users\Миша\Desktop\NewFolder"
#как я понял, вводим переменную, которая читает файлы в директории
dir_list = os.listdir(path)
#выводим файлы в консоль
print('Файлы в директории', path, ':')
print(dir_list)

#код не работает, т.к я использую онлайн компилятор
