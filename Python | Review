# исходный код:
#def factorial(n):
    #if n == 0:
        #return 1
    #else:
        #return n * factorial(n-1)
# обновлённый код
import numpy
try:
    n = int(input('Введите число, факториал которого вы хотите найти'))
    factorial = numpy.math.factorial(n)
    print('Факториал числа', n, '=', factorial)
except ValueError:
    print('Ошибка. Вы ввели нечисловое значение.')
#Есть возможность упростить исходный код через библиотеку numpy, вклюачающую в себя функцию factorial. Добавлено выведение ошибки при вводе нечислового значения. Код стал читабельнее, на мой взгляд.
