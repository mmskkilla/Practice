#исходный код
#def divide(x, y):
    #try:
        #result = x / y
    #except ZeroDivisionError:
        #return "Cannot divide by zero"
    #return result
#обновленный код
def divide(x, y):
    try:
        result = x/y
    except ZeroDivisionError:
        return "Error. Cannot divide by zero"
    except ValueError:
        return "Error. Non-numeric value"
    return result
#Теперь пользователь не может вводить нечисловые значения.
